name: testfilter
on:
  push:
    branches: 
     - 'master'
     - '**branch-**'
  pull_request:
  workflow_dispatch:
  
jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Run echo
        run: echo "${{ github.event }}"
