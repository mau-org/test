on:
  push:
    branches:
      - 'main'
  
jobs:
 test:
    runs-on: ubuntu-latest
    steps:
    - uses: okimaureen/zola-deploy-action@master
    env:
      GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
  
     
